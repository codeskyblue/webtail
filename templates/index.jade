//- vim: set ts=2 sts=2 sw=2 noexpandtab:

extends layout

block script
	//- script(src='/src/index.js', type='text/jsx')
	script(src='/js/homepage.js')
	link(rel='stylesheet', href='/css/homepage.css')
	//-script(src='/js/index.js')

block content
	.container(ng-controller='MainCtrl')
		//- h1 tail -f log {{name}}
		pre.log
			div(ng-class="line.selected ? 'line-selected' : 'line'", ng-repeat='line in logs')
				p.inner-line(ng-click='line.selected = !line.selected') 
					{{line.body}}
		//- div.logs(scroll-glue-bottom="glued")
		//- 	ul
		//- 		li(ng-repeat='line in logs')
		//- 			{{line.number}}: {{line.body}}
		//- div#bottom
		//- div#example
		//- pre.console
